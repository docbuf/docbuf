[package]
name = "docbuf-benchmarks"
version = "0.1.0"
edition = "2021"

[[bench]]
name = "benchmarks"
path = "benches/main.rs"
harness = false

[dependencies]
docbuf-core = { path = "../core" }
docbuf-tests = { path = "../tests" }
criterion = "0.5.1"
